/**
 * Demo event id used throughout the single-tenant prototype.
 *
 * When this app graduates to multi-tenant, replace reads of this constant
 * with a router param (e.g. ``/events/:eventId/...``) so the same UI can
 * drive any event.
 */
export const DEMO_EVENT_ID = "demo-event";
